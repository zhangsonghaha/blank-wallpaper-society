## 2026-06-01

### 登录/注册页重构

- 将用户设计的 HTML 登录页（含 GSAP 壁纸阵列开场动画）整合到 Next.js 项目
- 重写 `src/app/login/page.tsx`：合并登录/注册为一体，通过 mode 切换
- 保留原有设计的完整视觉风格：渐变光晕背景、壁纸阵列动画、卡片视差效果、密码可见切换、测试账号展示
- 完整接入项目已有认证体系：
  - 登录：`signIn("credentials", ...)` via NextAuth
  - 注册：`/api/auth/register` + Altcha 人机验证 + CSRF 防护
  - OAuth：GitHub/Google 第三方登录（动态检测可用性）
  - 错误处理：账号锁定、密码错误等
- `src/app/register/page.tsx` 改为重定向到 `/login?mode=register`
- 安装 `gsap` 依赖用于开场动画
- 注册页保留 test account 快捷填入功能

### 修复：登录状态检测 + ClientFetchError

- 添加 `useSession()` 已登录检测，已登录用户自动 `router.replace("/")` 跳首页
- 开场动画延迟到确认未登录状态后才播放，避免已登录用户看到动画闪烁
- 添加 session 确认中加载态（显示 spinner + "验证登录状态..."）
- 将 GSAP animation useEffect 的 dep 改为 `[sessionReady]`
- 注册页重定向确认正常工作（server component redirect）
- `ClientFetchError` 问题分析：可能是 `auth.ts` 中 `await buildProviders()` 的数据库查询在服务端初始化时返回错误导致。如继续出现，需要检查数据库连接配置（`DB_HOST`、`DB_USER` 等环境变量）

### 修复：登录页布局重叠/不美观

- 登录页重复的自定义导航栏（`.auth-nav`）已移除，改用 Layout 的 Navbar 组件
- 页面布局从 `position: fixed; inset: 0` 改为正常文档流，适配 Layout 的 `pt-16` 内边距
- `.auth-stage` 使用 `flex + min-height: 100%` 在 Layout 容器内垂直居中卡片
- 移除底部水印文字（Layout 已有 Footer）
- GSAP 动画移除对已删除导航栏的引用
- 响应式媒体查询精简，只保留必要的卡片和壁纸网格规则
- 已登录用户的 session 检测加载态使用 `width: 100vw; height: 100vh` 覆盖全屏（避免导航栏闪烁）

### 新增：随机壁纸 + 后台可配置

- 创建 `src/lib/login-wallpapers.ts`：60 张 Unsplash 精选壁纸池 + Fisher-Yates 洗牌算法
- 创建 `GET /api/auth/login-wallpapers`：服务端按配置返回随机壁纸列表
- 打开登录页时从 API 获取 24 张随机壁纸，每次刷新不同
- 后台设置增加「登录页壁纸」区域：
  - 壁纸来源：Unsplash 精选（默认）/ 自定义图片
  - 「自定义图片」时，URL 换行输入，自动转为 JSON 数组存储
  - 加载时 JSON 数组自动转为换行文本显示
